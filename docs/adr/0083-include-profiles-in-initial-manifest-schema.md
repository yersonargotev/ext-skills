# Include profiles in the initial manifest schema

Because Harness development has not started and no released manifest schema exists yet, catalog profile fields will be included in the initial Éxito Plugin Manifest schema instead of introducing a `1.1` schema version. A schema version bump is reserved for changes made after a schema has been implemented, released, or consumed by catalog plugins.
