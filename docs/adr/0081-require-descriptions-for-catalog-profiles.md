# Require descriptions for catalog profiles

The Catalog Profile Registry will include user-facing descriptions for each catalog profile value instead of relying on canonical IDs alone. This keeps curated profile language understandable during catalog discovery and prevents profile IDs from becoming unexplained internal jargon.
