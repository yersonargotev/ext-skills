# Include catalog profiles in v1 discovery scope

Catalog profiles are part of v1 catalog discovery as explicit filters and visible metadata, because they help users narrow and understand the curated catalog without installing anything, changing the default all-plugins catalog view, or mixing with installation targets. Installing from a profile remains out of scope, so profiles guide discovery only and do not become plugin bundles, automatic selections, or target compatibility rules.
