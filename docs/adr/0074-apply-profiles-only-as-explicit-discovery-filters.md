# Apply profiles only as explicit discovery filters

Catalog profiles initially apply only when catalog discovery includes explicit profile filters, while the default catalog view remains unprofiled and shows all catalog plugins. This keeps the baseline catalog predictable, avoids hiding approved plugins behind inferred role or technology assumptions, and preserves profile curation as an opt-in discovery aid rather than an installation or visibility rule.
