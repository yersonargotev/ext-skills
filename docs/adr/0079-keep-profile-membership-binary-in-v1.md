# Keep profile membership binary in v1

Harness catalog profile membership is binary in v1: a plugin either belongs to a profile or it does not. We will not model primary/secondary profiles, profile scores, or strength-based membership yet because those concepts add another governance dimension on top of the curated profile vocabulary before the product has evidence that weighted membership is needed.
