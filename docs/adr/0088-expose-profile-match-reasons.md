# Expose profile match reasons

Profile-filtered catalog discovery will show the match-count reason behind ranked results, such as matching 2 of 2 selected profiles before plugins matching 1 of 2, because users should understand why all-profile matches appear ahead of partial matches. Sorting by profile match strength must not be unexplained magic; the output should make the ranking reason visible without turning profile membership into a weighted score or installation rule.
