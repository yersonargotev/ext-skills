# Curate catalog profile vocabulary centrally

Catalog profile axes and allowed profile values are centrally curated rather than freely declared by individual plugins. This keeps the catalog language coherent and prevents semantic drift where equivalent ideas split into competing labels such as `frontend`, `front-end`, `fe`, `web`, or `ui`.
