# Use explicit profile membership axes

Catalog profile membership will use explicit profile axis and profile id pairs, such as `role:frontend` or `technology:react`, instead of a bare list of strings. This avoids ambiguous labels across different classification dimensions and keeps profile membership readable without requiring future readers or tools to infer what each label means.
