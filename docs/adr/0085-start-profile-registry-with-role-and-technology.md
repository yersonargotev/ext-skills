# Start profile registry with role and technology

The v1 Catalog Profile Registry starts with only the `role` and `technology` axes, because those two dimensions cover the initial discovery contexts without turning profiles into an open-ended tagging system. Additional axes require later explicit decisions so new dimensions remain curated, intentional, and separate from recommendation level, installation target, or other catalog concerns.
